{
  "name": "klickwort",
  "version": "1.0.0",
  "description": "Lookup words with wiktionary by doubleclicking on them.",
  "main": "background.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "webpack --mode=production",
    "package": "npm run build && web-ext build --overwrite-dest"
  },
  "author": "janoande",
  "license": "ISC",
  "dependencies": {
    "langs": "^2.0.0"
  },
  "devDependencies": {
    "eslint": "^6.7.2",
    "webpack": "^4.41.2",
    "webpack-cli": "^3.3.10"
  }
}
