{
    "private": true,
    "scripts": {
        "build": "webpack --mode=production",
        "package": "npm run build && web-ext build --overwrite-dest",
        "webext": "web-ext run --verbose",
        "lint": "npx eslint src"
  },
  "author": "janoande",
  "license": "MIT",
  "dependencies": {
    "langs": "^2.0.0"
  },
  "devDependencies": {
    "eslint": "^6.7.2",
    "webpack": "^4.41.2",
    "webpack-cli": "^3.3.10"
  }
}
